# OpenAI API Key (required)
OPENAI_API_KEY=sk-your-openai-key-here

# OpenAI Model (DEPRECATED - không dùng nữa)
# Model được chọn từ UI (models.json), không cần set trong env
# OPENAI_MODEL=gpt-4o-mini

# System Prompt (optional)
SYSTEM_PROMPT=You are a helpful assistant.

# Server Port (optional, default: 3001)
PORT=3001

# Debug mode (optional, set to "true" to enable debug logs)
DEBUG=false

# Clerk Authentication
# Lấy keys từ: https://dashboard.clerk.com → API Keys
# ⚠️ QUAN TRỌNG: Thay thế bằng keys thật của bạn!
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your-clerk-publishable-key-here
CLERK_SECRET_KEY=sk_test_your-clerk-secret-key-here

# Production Settings
# NODE_ENV=production (tự động set bởi hosting platform)
# ALLOWED_ORIGINS=https://your-frontend-domain.com,https://your-backend-domain.com (comma-separated)
# UI_API_PROXY_TARGET=https://your-backend-domain.com (cho frontend build)
